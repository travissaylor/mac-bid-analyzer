{
  "name": "mac-bid-analyzer",
  "version": "0.1.0",
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "@types/chrome": "^0.1.40"
  },
  "engines": {
    "bun": ">=1.0"
  },
  "private": true,
  "scripts": {
    "start": "bun run src/cli.ts",
    "typecheck": "bun x tsc --noEmit",
    "test": "bun test",
    "build:extension": "bun run build.ts"
  },
  "type": "module",
  "dependencies": {
    "@google/genai": "^1.46.0",
    "openai": "^6.32.0",
    "telegraf": "^4.16.3"
  }
}
